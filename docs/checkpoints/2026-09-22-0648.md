# Checkpoint — 2026-09-22 06:48

## Session summary

Continued from the 2026-09-21-2200 checkpoint. Two major arcs closed this session: finishing round 1 of the security/performance review (the last two tasks, P0-1 and #230, plus the exec-track chain P2-1/P3-2 that depended on P0-1), then running a full second review round end to end.

**Round 1 close-out.** Task #212 (P0-1, the release-blocking unrestricted-conhost finding) and task #230 (window-flash-fixes, an unrelated user request) had both timed out mid-run in prior turns after burning through their rush budgets on internal sub-agent fan-out without committing. Both worktrees held complete, correct, uncommitted diffs. Rather than spending a third rush cycle on either, the diffs were reviewed directly, verified (build/vet/gofmt/lint/race/full suite), committed, and cherry-picked into main: `6751a41` (P0-1: `ShieldConhost` applying `Shield()`'s three-object shut to a console host found by parentage) and `921810c` (window-flash-fixes: a windowless console-holder harness replacing the racy AllocConsole-then-hide pattern, a shared `internal/win/quietexec` package, `--own-console`'s e2e test gated behind `WUSERBOX_E2E_OWN_CONSOLE`). This freed task #213 (P2-1, exec track), delegated to a fresh rush worktree, which landed as `b55a33a` (tracked relay-drain lifecycle replacing the fixed 250ms sleep). Its own stress-testing surfaced a real, previously-unknown race in the *already-merged* P0-1 code (`shutThreadsAlreadyRunning` in `internal/win/proc/shield.go`): a fresh conhost's thread can fail to appear in a Toolhelp32 snapshot taken the instant its pid is known, and a thread born between the token-default-DACL write and the thread walk answers `ERROR_ACCESS_DENIED` (correctly, since it's already shut) rather than the walk's expected success — reproduced directly via 30-40x stress runs, fixed with a bounded retry-on-empty-walk plus treating an owner's `WRITE_DAC` refusal as proof-of-already-shut, landed as `a0b4722`. Task #229 (P3-2, launch-trace phase splitting) followed, landed as `e967f36` (`command_launch` split into `child_create`/`child_resume`/`child_wait`/`job_close` and the account path's `stub_create`/`stub_resume`/`stub_wait`/`bridge_drain`, renamed umbrella `command_lifetime`, pid+run-id on every record) — its own close test caught a real bug in the new code itself (the `FromEnv` file branch forgetting to set the run identifier) before landing. This closed every finding from `docs/reviews/security-performance-review-2026-09-20.md`.

**Round 2.** The user supplied a second review report, `docs/reviews/security-performance-review-2026-09-22-round2.md` (already committed to main as `ba0f8a2` by a separate process before this session touched it), finding 0 new P0s, 2 P1s (both release-blocking), 5 P2s, and 3 P3s (one of which, trace-phase P3-2, was already in flight and absorbed into the existing task rather than duplicated). Decomposed into 6 tasks, grouped by file overlap: R2-P1 (the two P1s combined, since the review explicitly required coordinating them — HPCON lifetime races in `internal/sandbox/exec/console.go`), R2-P2-1 (same file, sequenced after R2-P1), R2-P2-2/P2-3 (conhost test-coverage hardening, `internal/e2e/conhost_test.go`), R2-P2-4 (warm-copy de-quadratic-ification, `internal/policy/profile`), R2-P3-1 (sweep's pinned-snapshot memory, `internal/win/acl/sweep.go`+`pinned.go`), R2-P2-5/P3-3 (audit correctness+dedup, `internal/win/acl/entries.go`+`internal/cli/inspect/audit.go`). Five tracks launched in parallel worktrees; one rush session (`p3-2-launch-trace`, already running from round 1) hit a genuine rate-limit failure from the simultaneous launch and was resumed successfully. All six landed, each independently zero-trust-verified (full diff read, manual correctness derivation, build/vet/gofmt/golangci-lint, `-race` stress at `-count≥5` — `-count=10`/`-count=40` for the most concurrency-sensitive ones, full `go test ./...`) before cherry-picking into main:

- `68d3c2d` (R2-P1) — one mutex-guarded owner for the relay's HPCON (`hpcMu`+`hpcClosed`+`resizes` WaitGroup, neither native call made under the lock), and `finish`'s drain-ceiling timer now races the close itself on its own goroutine instead of starting only after a synchronous close returns. Personally reproduced the real, documented pre-24H2 `ClosePseudoConsole` hang on this machine (Windows 10.0.19045) 3/3 times and confirmed the ceiling bounds it.
- `eeab4d6` (R2-P2-1) — `shutBirthConsoleHost`'s `GetConsoleProcessList` fail-open closed: only a confirmed `ERROR_INVALID_HANDLE` (measured, "no console at all") is a safe no-op now; any other failure refuses the run before `Shield()`.
- `ab26360` + `415efad` (R2-P2-2/P2-3) — the conhost boundary e2e test's own-console leg gated behind `WUSERBOX_E2E_OWN_CONSOLE`; the thread-shield prowler now asks every live thread (not just the first) and a new test (`TestAThreadBornIntoAShieldedProcessIsBornShut`) directly constructs a thread born between the token-default write and the walk, verifying from a restricted seat that both dangerous doors are actually closed.
- `9a1cb8a` (R2-P2-4) — `stillNamed`/`recordVouches` each hold one `placeResolver` across their whole question instead of one per pairwise comparison; the resolver itself indexes directory children by name and memoizes alias-branch answers. Verified mathematically equivalent to the old pairwise semantics; a counter test proved 6 resolvers/6 opens on a 6×6 warm respell versus the old shape's 42/42.
- `80accec` (R2-P3-1) — the sweep's pinned-path keep-list now retains only objects the sandbox's own account owns (the only ones `classifyNarrow`/`capObject` ever consult), riding the same `GetNamedSecurityInfo` call the first pass already makes rather than a second read; explicitly does NOT claim O(window) for the whole sweep.
- `41a6a76` (R2-P2-5/P3-3) — `heldBy` now excludes `InheritOnly` ACEs when answering about the object itself (real correctness bug: `--audit` was misreporting both directions); a new `WritablePass` type resolves the Everyone/Users SIDs once per operation and answers both questions from one descriptor read instead of three.

Two anomalies surfaced during R2-P2-1's final verification and were investigated, not dismissed: a GC-worker segfault (register dump) in `internal/win/acl` during one `go test ./...` run, and one failure of `TestALiveConsolesHungCloseIsStillBoundedByTheCeiling` (from the already-merged R2-P1) during another. Both reproduced 0-for-many in isolation (6/6 and 5/5 respectively) and 0-for-2 on subsequent full-suite reruns; attributed to the session's own sustained heavy load (many consecutive full-suite runs back to back) rather than either task's diff, consistent with this session's established pattern of load-induced flakes.

`/babygoal`'s babysit cron (`f1c8dc66`) self-deleted this session once the TaskList emptied after R2-P2-1 landed — every task from both review rounds is `completed`, none pending or in_progress.

## Active goal

None set through `/goal`. TaskList drove the work; it is now empty.

## TaskList

Empty — all tasks from both review rounds (#212, #213, #222–#236, minus #214–#221 which were deleted and recreated as #222–#229 earlier in the session) are `completed`. No pending, in_progress, or blocked work remains.

## Decisions

- **Took over three timed-out rush sessions directly (round-1 P0-1, round-1 #230, round-2 P2-1's rate-limit failure) rather than spending further rush cycles on them** — each had a complete, correct, uncommitted diff in its worktree; the coordinator's own verification discipline (read diff, build/vet/lint/race/full-suite) is the same regardless of who writes the commit message, so re-running rush only to get a commit was pure overhead once the code was already sound.
- **Combined round-2's two P1s (HPCON-lifetime races) into one task** rather than two, per the review's own explicit warning that fixing them independently risked a new deadlock between the close-ceiling and the resize-ownership fix.
- **Did not duplicate round-2's P3-2** (trace-phase splitting) as a new task — it was the same finding as round-1's still-in-flight #229, absorbed by resuming that session with the round-2 report's one addition (the `internal/cli/setup` slot-wait phase) noted for the agent.
- **Investigated, rather than assumed, two anomalies during final verification** (a segfault and a live-hang-test failure) that appeared only under sustained full-suite load and never in isolation — reproduced clean 11 times combined before accepting them as load-induced and proceeding to merge.
- **The GetConsoleProcessList fail-closed fix (R2-P2-1) treats only a specifically-measured errno (`ERROR_INVALID_HANDLE`) as the safe no-op**, refusing on every other failure, rather than guessing at a broader set of "probably fine" error codes — matches this session's standing fail-closed-on-uncertainty discipline.

## Open questions

- None blocking. The entire review-fix chain (`5e7a02c` through `eeab4d6`, i.e. everything after `4fd109a`) is local-only on `main`, not yet pushed to `origin/main` — pushing and confirming CI green has not been explicitly requested for this batch.
- Whether to reinstall the newly-built wuserbox binary to the system — not requested for this build.

## Repo state

```
?? docs/checkpoints/2026-09-19-1938.md
```
```
eeab4d6 Refuse the run when the birth console's guard cannot read its answer, instead of reading every answer it cannot understand as permission to skip the shield: shutBirthConsoleHost treated any GetConsoleProcessList response but one as "no console of this process's own, nothing to shut", and zero -- which per Microsoft's contract is a failed call, not an empty one, because every console has at least one process attached and a successful call never answers it -- was folded in with the inherited-console no-op, so a call failing for an unidentified reason silently skipped the birth host's shield and the stub went on to narrow its token and start the untrusted program with the host's state unknown, the fail-open shape P0-1 closed everywhere else on this exact path (review round 2, P2-1).
9b4208b Give the relay's HPCON one owner and point the drain ceiling at the whole teardown, not just the drain: finish used to call ClosePseudoConsole synchronously and only started the five-second clock after it returned, but before Windows 11 24H2 that call is documented to block while its output pipe has nowhere to drain -- so the ceiling never covered the one call that can hang forever, and a stub whose bridge reader stalled held its run slot indefinitely, refusing every later run of that sandbox -- while the resize pump handed r.hpc straight to ResizePseudoConsole with nothing stopping closeConsole from freeing the console between the field read and the call, a Go data race and a native use-after-free against the heap object the close releases.
41a6a76 Stop --audit from counting entries Windows never applies to the object itself, and from paying for the same answer three reads at a time
80accec Narrow the keep-list's pre-change snapshot to the objects the narrowing will still consult
415efad Make the thread half of the conhost shield prove itself thread by thread instead of trusting one thread and one refusal
```

`main` is pushed to `origin/main` only through `4fd109a` (console-relay CI fix, confirmed green in an earlier session). Everything from `5e7a02c` onward — the entire round-1 and round-2 review-fix chain, 20+ commits — is local-only. No worktrees or branches from this session remain; `git worktree list` shows only pre-existing sibling worktrees from unrelated earlier sessions, untouched. No cron jobs remain armed. The previous checkpoint file (`docs/checkpoints/2026-09-19-1938.md`) stays untracked, left alone per this skill's own rule.
