# Checkpoint — 2026-09-20 15:46

## Session summary

Continued from the 2026-09-20-0702 checkpoint. Ran a second-round security review (`sx`, effort=max) over the sandbox at `50c8db4`; nothing new found, merged. The user hit `wuserbox cy` (Crush, interactive chat mode) failing with the same underlying cause as the earlier `cx` bug -- diagnosed as the same "no real console for the sandboxed process" issue, not a new one. Launched an `fm` agent (in an isolated worktree, per this session's own worktree-isolation rule) to investigate a same-window fix for this: found Windows Terminal's tab-handoff mechanism is account-scoped and unreachable from the sandbox account (NOT VIABLE), console objects carry no security descriptor at all so no ACL grant could ever bridge the cross-account `AttachConsole` denial (NOT VIABLE, measured: `GetKernelObjectSecurity` answers `ERROR_NOT_SUPPORTED`), but a third avenue -- a ConPTY (pseudo console) built *inside* the sandbox account, relayed as bytes over the account-crossing bridge that already exists -- looked genuinely viable, partially measured. Merged that investigation doc.

The user then asked (`/babygoal ... /rush`) to actually build that pseudo-console relay as same-window replacement for `--own-console`'s separate window. Decomposed into 8 sequential tasks (file-overlap forced sequential, not parallel), armed `/babysit` at 15m, and ran every task through real `rush run` invocations in dedicated worktrees, personally verifying (build/vet/lint/full `go test ./...`) before merging each one, per this session's zero-trust discipline:

- **#204** measured live, under a real sandbox account token (the leftover scratch account `wub-25e03ccf5218477d` from an earlier session, no fresh UAC needed): `CreatePseudoConsole` + `CreateProcessAsUserW` + `STARTUPINFOEX` + `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` all work under the restricted token `AsSandbox` builds. VIABLE, confirmed.
- **#205** built the mechanical half: `takeConsoleRelay()` (pseudo console before `Shield()`, same measured window as `--own-console`), `proc.RunWithConsole` (the new launch form), `WUSERBOX_CONSOLE_RELAY` env var, mutual exclusion with `--own-console`.
- **#206** wired the byte crossing: no new pipes needed -- the stub's own existing stdio bridge pipes (built for the non-relay case) *are* the relay's crossing; `pumpRelay` on the stub side, `relayConsoleModes`/raw input mode on the operator side.
- **#207** added resize (a third dedicated pipe, polled `GetConsoleScreenBufferInfo` + a new `lock.PassHandleTo`/`PrepareHandleTransfer`/`AdoptTransfer` handle-handoff primitive, parallel to the existing lease-only `PassTo`) and Ctrl-C forwarding (`relayWatchInterrupts`, a second `signal.Notify` listener writing the raw byte `0x03` into the relay's input bridge -- found and fixed a real bug along the way: `ENABLE_PROCESSED_INPUT` staying on meant a bare Ctrl-C press in relay mode reached wuserbox but never the program at all before this).
- **#208** added a `SetConsoleCtrlHandler`-based kill-restore handler so the operator's console mode survives `CTRL_CLOSE`/`LOGOFF`/`SHUTDOWN` (not just ordinary returns); confirmed no `os.Exit` in the reachable call chain skips the ordinary defer path, so nothing else needed fixing there.
- **#209** added the `--console-relay` CLI flag (mirroring `--own-console`'s exact shape), parse-time mutual exclusion (in addition to the stub's own backstop), docs.
- **#210** surveyed the by-then-already-extensive test coverage for genuine gaps rather than padding; found and closed exactly one (the relay's kept pipe ends' `HANDLE_FLAG_INHERIT` hygiene was asserted in a comment but never read back); confirmed the account-crossing e2e test could only be compiled/skipped locally (no admin), same as every other admin-gated e2e test in this repo -- CI is where it actually runs.
- **#211** (in progress): local `go build`/`vet`/`gofmt`/`golangci-lint`/`go test ./...` all clean, pushed 9 commits to `origin/main` (`e7e62bb`..`ae06fee`). **CI came back red** on the one test that had never actually run until CI's real admin rights exercised it: `TestAProgramThroughTheConsoleRelaySeesARealTerminalAndRelaysItsBytes` failed both its assertions (`IsInputRedirected` reported `True` instead of `False`; a fed stdin line never reached the program). Root-caused it myself, live, without needing CI or admin rights: reproduced locally (same-account, no elevation) with a scratch probe launching real `powershell.exe` through `proc.RunWithConsole`, and found `CREATE_NO_WINDOW` was missing from that function's process-creation flags. Without it, `CreateProcessAsUserW` leaves the child's `GetStdHandle` unset even though `CONIN$`/`CONOUT$` open and answer `GetConsoleMode` correctly by name -- and PowerShell's own `[Console]::IsInputRedirected` reads `GetStdHandle` directly rather than doing the CONIN$/CONOUT$-by-name fallback this repo's own test probes always did by hand. This directly disproved a claim baked into `RunWithConsole`'s own doc comment and into `stdin_bridge_test.go`'s `fixupStdinFromConin`/`fixupStdoutFromConout` comments since #205 ("C-runtime apps never notice" -- measured false: same-account PowerShell without the flag showed the identical broken behavior). Fixed `RunWithConsole`'s flags (`createSuspended | extendedStartupInfoPresent | createNoWindow`), corrected both misleading comments, added a same-account (no elevation needed) regression test `TestRunWithConsoleGivesTheChildAWorkingGetStdHandle` in `internal/win/proc/relay_test.go`. That test itself needed one iteration: reading the *rendered console output pipe* for the report was flaky because PowerShell's own buffered `Write-Output` doesn't reliably flush to the pty before a fast script exits (a separate, real, but out-of-scope .NET buffering quirk) -- switched the test to write its report through `Set-Content` to a file instead, matching the exact shape that verified the real fix. Full local verification (build/vet/lint/`go test ./internal/win/proc/...` targeted, then a full `go test ./...`) was in progress -- **the full-suite run had not yet completed** -- when this checkpoint was written. Not yet re-pushed.

## Active goal

None set through `/goal`. TaskList drives the work.

## TaskList

### in_progress
- #211 Merge console-relay feature, push, get CI green (blockedBy: none) -- CREATE_NO_WINDOW fix applied and locally verified for the specific failing test; full `go test ./...` re-run was still running when this checkpoint was written; not yet pushed.

### recently completed (this session, console-relay feature)
- #204 Measure ConPTY under the real sandbox account token
- #205 Build stub-side pseudo console + STARTUPINFOEX launch path
- #206 Relay pseudo console bytes across the operator/stub bridge
- #207 Forward console resize and Ctrl-C into the pseudo console
- #208 Restore operator console mode on every exit path
- #209 Wire the console-relay CLI flag and update docs
- #210 Add tests for the console-relay feature

## Decisions

- **The console-relay feature (`--console-relay`) reuses the existing stdio bridge pipes as its byte-crossing mechanism** rather than building new dedicated pipes for output/input -- only resize got a genuinely new, dedicated third pipe, because it needed its own wire format and couldn't be multiplexed onto the transparent byte streams without turning every keystroke into a parse case.
- **`CREATE_NO_WINDOW` is required, not optional, for `RunWithConsole`** -- its absence was the actual CI failure's root cause, disproving this session's own earlier assumption (baked into multiple commits' doc comments) that ordinary C-runtime console programs recover from a missing `GetStdHandle` automatically. They do not; only this repo's own hand-written test probes did the CONIN$/CONOUT$-by-name fallback themselves.
- **Root-caused the CI-only failure without needing CI or admin rights**, by isolating the actual variable (launch flags) from the parts of the bug that genuinely did need account-crossing/admin (none, as it turned out) -- a same-account local reproduction was sufficient and much faster to iterate against.
- Every CI-only failure this session (here and in the earlier round10 arc) has been root-caused for real, never dismissed as a flake or silently worked around.

## Open questions

- None blocking. Once the full local `go test ./...` re-run confirms green, the remaining steps are: push the `CREATE_NO_WINDOW` fix commit, watch CI, and (if green) mark #211 and the babysit cron complete.
- Whether to reinstall the newly-built wuserbox binary to the system once this lands (the user has asked for this routinely throughout the session) -- not yet requested for this specific build.

## Repo state

```
 M internal/win/proc/relay_test.go
 M internal/win/proc/run.go
 M internal/win/proc/stdin_bridge_test.go
?? docs/checkpoints/2026-09-19-1938.md
```
```
ae06fee Hold the relay's kept pipe ends to the hygiene its comments claim: read back, not argued
33c5f31 Name the relayed console in the operator's own words: --console-relay, parsed beside --own-console
a3cd218 Put the operator's console back from the kills a handler can reach: the modes restored inside Windows' window, the event passed on untouched
d9c374c Give the relayed console the operator's Ctrl-C and its window's size: the interrupt forwarded as the byte a keyboard sends, the size on a third pipe
3a8fd63 Carry the windowless console across the account line as bytes: pumped on the stub's own bridges
6dd61ef Build the mechanical half of a windowless console: taken before Shield, attached through RunWithConsole
e7e62bb Close the pseudo console question under a real sandbox token: viable, through the launch call wuserbox actually uses
3c2ad49 Say why the sandbox's console cannot be the operator's, and where one door is left
```

`main` is pushed to `origin/main` at `ae06fee`; that push's CI run (`35512949943`) came back **red** on `TestAProgramThroughTheConsoleRelaySeesARealTerminalAndRelaysItsBytes`. The fix (`CREATE_NO_WINDOW` in `internal/win/proc/run.go`, plus the new regression test and corrected comments) is written and locally verified for the specific test but **not yet committed or pushed** -- `git status` above shows it as uncommitted working-tree changes. The previous checkpoint file (`2026-09-19-1938.md`) remains untracked, left alone per this skill's own rule.
