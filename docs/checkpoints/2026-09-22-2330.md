# Checkpoint — 2026-09-22 23:30

## Session summary

Continued from the 2026-09-22-1517 checkpoint (which covered the CI-greening arc and round-4 review closure). This session closed three more security/performance review rounds end to end — round 5 (7 findings), round 6 (7 findings), round 7 (6 findings) — each via `/babygoal` + `/wrush`: decompose the review report into file-disjoint TaskList entries, arm `/babysit`, delegate each finding to a rush sub-agent in its own git worktree, personally zero-trust-verify every diff (read it line-by-line, rebuild build/vet/lint/`-race`/full-suite myself, never trust the sub-agent's own report), merge into the primary checkout, remove the worktree. Between rounds, the user also asked to make the GitHub repo public (done, after a secret-scan of the full history found nothing sensitive) — this incidentally fixed a CI billing block from a prior push, since public repos get free GitHub Actions minutes.

**Round 5** (7 tasks, all merged): P1-1 fail-closed `Identity`/`SandboxGroup`/`IdentifierAlone` contract for `ERROR_NONE_MAPPED` (`fd88f9c`, plus fixing a real bug this uncovered: `internal/sandbox/exec`'s trace-driver subprocess wasn't getting the synthetic-identity promise, causing a real 90s CI timeout — fixed by moving the promise-setting call ahead of role dispatch in its `TestMain`); P3-2 scoped `sid.Free` in token/account helpers (`b0c9969`); P2-2 alias-index idempotency regression (`67885d1`); P2-1 warm-copy stretch invalidation only on real structural mutation, not byte rewrites (`f3209ca`); P3-1 one shared scratch buffer per copy operation (`2f17981`).

**Round 6** (7 tasks, all merged): P1-1 Go-owned SID buffer lifetime — typed pointer + `KeepAlive` instead of bare `uintptr` (`4eacc19`); P2-1 `clearEntry`/`mirror.go` distinguish `os.IsNotExist` from other Lstat errors (`51993b6`); P2-2 SandboxGroup fail-closed contract extended to the second (member-lookup) SAM stage, not just the first (`3c7d097`); P2-4 account-comment-embedded creator SID so a second operator can't silently delete the first operator's live account (`35d8ad3`); P3-2 operation-local `pathIndex` for O(R+G) grant-path resolution instead of O(R·G) (`a5fbe57`); P2-3 `handDown`/`handDownEntry` adapt HomeTop's inherited ACE flags by object kind and generation instead of copying the parent's raw flags verbatim (`f22dc51`); P3-1 `retract` — scoped resolver-cache invalidation on real clears instead of dropping the whole stretch (`2994b27`). One process-integrity anomaly investigated mid-round: P1-1's rush session hit three consecutive "queued behind an active run" failures under the same session id; `rush sessions reap`/`kill` didn't clear it, so the fix was resumed under a fresh session id (`r5-acl-identity-v2`) pointed at the same worktree — the resulting commit's provenance (whether an orphaned prior attempt or a sub-agent produced the final commit/amend) was investigated and the actual diff content was independently re-verified from scratch rather than trusted.

**Round 7** (6 tasks — 5 merged, 1 in flight): P1-1 (`4c552af`) replaced round-6's `textConverter` interface (whose indirect dispatch defeated the compiler's `//go:uintptrescapes` treatment, leaving a movable-stack-slot address crossing the Go/native boundary as a bare number — confirmed via `-gcflags='-live -m=2 -S'` showing `stack object text` feeding an indirect `CALL DX`) with a concrete func-typed seam whose production value calls `LazyProc.Call` directly inside its own body; independently reverified via the same compiler flags post-fix (`moved to heap: text`, `//go:uintptrescapes` on both conversions, static `CALL syscall.(*LazyProc).Call`). P2-1 (`5590f5e`) cleanup-glob `lookAt` fold fixed the same way round-6 already fixed `clearEntry` — `mayMatchDescendant` moved ahead of the Lstat call, `IsNotExist` vs other-error now distinguished. P2-2 (`f4e875c`) `holdsAny` rewritten from unordered allow/deny mask subtraction to sequential per-bit first-decisive-ACE-wins evaluation, matching real Windows `AccessCheck` order semantics. P2-3 (`6c9fe3a`) `generationsBelow` now handles a root that already ends in a path separator (drive roots, UNC shares) instead of demanding a second one. P3-2 (`5eeea19`) `TakeBack` now memoizes `sandboxGroup`'s expensive `sid.Name` lookups per operation, keyed by SID *content* (via `CopySid` into a fixed buffer, never by native pointer address, since descriptors are freed and addresses get reused) — while verifying this one myself I found and root-caused a real test bug (an `AllocsPerRun` assertion expecting exactly 0 allocations on a cache hit, when the key-formation `CopySid` call itself unavoidably forces a heap escape via `//go:uintptrescapes` on every call, hit or miss) and fixed the test in place to compare against the key's own baseline cost instead of a literal zero, rather than leaving it broken or silently weakening it.

**Round 7's P3-1 (task #257) is the one open item.** Its worktree (`worktrees/r7-retract-reverse-index`, still present) hit "Context deadline exceeded" on its first attempt with a substantial uncommitted diff already in place (`fold.go` +294/-, plus `partial_test.go`/`place_test.go` changes) that compiles cleanly. The stale lock was reaped and the session was resumed under the *same* session id (not a fresh one — unlike round 5's P1-1, this session's lock cleared normally) with a detailed follow-up prompt describing exactly what's already there and what's left (finish/verify the reverse-index implementation, re-pin `TestAForgetThatTakesHalfTheRecordBackRetractsItsAnswersNotRebuilds`'s children-count expectations from quadratic to near-linear, run the full verification battery, commit). That resumed run (`bfd2oxn7j`) was still in progress — confirmed alive via `rush sessions why` (heartbeat 9s) — when this checkpoint was requested.

## Active goal

None set through `/goal`.

## TaskList

### in_progress
- #257 P3-1: снизить стоимость retract's directory-invalidation (reverse index вместо полного map scan) — round-7 finding, worktree `r7-retract-reverse-index` has an uncommitted, compiling diff; rush session resumed and running as of this checkpoint.

### recently completed
- #258 P3-2: memoize trustee SID→name разрешение в revoke (round 7, `5eeea19`)
- #256 P2-3: расчёт поколения не должен отвергать потомка корня диска (round 7, `6c9fe3a`)
- #255 P2-2: audit порядок ACE (round 7, `f4e875c`)
- #254 P2-1: cleanup glob fail-closed (round 7, `5590f5e`)
- #253 P1-1: typed native adapter для SID syscall seam (round 7, `4c552af`)
- (rounds 5 and 6's 14 tasks, all completed and merged — see session summary)

## Decisions

- **Made the GitHub repo public** on explicit user request, after scanning the full git history for common secret patterns (private keys, API key literals, AWS keys, GitHub tokens) and finding nothing — this also resolved a GitHub Actions billing block from a prior push, since public repos get free CI minutes.
- **When a rush session hits "queued behind an active run" repeatedly even after `reap`/`kill` clear the lock, switch to a fresh session id pointed at the same worktree** rather than continuing to retry the same id — this recovered round 5's P1-1 after three failed attempts under the original id.
- **When a completed session's own diff and tests look done but a specific assertion fails under independent re-verification, root-cause and fix it in place rather than deferring or trusting the session's self-report** — applied to round 7's P3-2, where an allocation-count test asserted an architecturally unachievable "0 allocations on a cache hit" (the key-formation native call unavoidably escapes to heap every call); fixed to compare against the key computation's own baseline cost.
- **Continued the established zero-trust merge discipline unchanged across all three rounds**: every sub-agent's diff read in full before merging, every build/vet/lint/race/full-suite claim independently reproduced, no commit merged on the strength of the sub-agent's own report alone.

## Open questions

- None blocking. Round 7's P3-1 (#257) is the only unfinished item; its rush session was confirmed alive and progressing as of this checkpoint — the natural next step on resume is to check `rush sessions why r7-retract-reverse-index` (from inside that worktree) and either wait for completion or investigate if it has stalled.
- Round 7's other 5 findings are merged into `main` but **not yet pushed** — `origin/main` is 25 commits behind local `HEAD` as of this checkpoint (spanning the tail of round 5 through round 7's first 5 merges). No push has been requested for this batch yet.

## Repo state

```
D:/dev/go/wuserbox                                    6ecacab [main]
D:/dev/go/wuserbox/worktrees/r7-retract-reverse-index bf82142 [r7-retract-reverse-index]  (round-7 P3-1, in flight)
(all other sibling worktrees are pre-existing and unrelated to this session)
```
```
6ecacab Merge branch 'r7-revoke-trustee-memo'
5eeea19 Answer the sandbox-group question about a trustee once per identifier for the whole revoke instead of once per eligible entry on every object...
c40b936 Merge branch 'r7-audit-ace-order'
0ded3da Merge branch 'r7-cleanup-fail-closed'
9392249 Merge branch 'r7-sid-typed-adapter'
```

`main` is 25 commits ahead of `origin/main` (not pushed). Working tree of the primary checkout is clean. One worktree remains checked out (`r7-retract-reverse-index`, task #257, in progress) — every other round-5/6/7 worktree has been merged and removed per the established `/wrush` discipline. `babysit` cron `60266e11` is still armed (round 7's heartbeat) and will self-delete once #257 completes and the TaskList empties.
