# Checkpoint — 2026-09-21 22:00

## Session summary

Continued from the 2026-09-20-1546 checkpoint. Closed out task #211 (console-relay CI fix): the
first attempted fix (adding `CREATE_NO_WINDOW` to `RunWithConsole`) was proven wrong by full local
test evidence — it silently detached the child from the named pseudo console onto a separate hidden
one instead of fixing `GetStdHandle`, breaking the relay itself (empty pty output, a hung pump test)
while making a narrow local regression test pass for the wrong reason. Reverted it, then delegated a
fresh, better-grounded investigation via `/rush` (worktree `p0-1-conhost-shield`... no, that name was
reused later; the CI-fix worktree was `console-relay-stdhandle-fix`), which found the real cause:
`RunWithConsole` never set `STARTF_USESTDHANDLES`, so the child inherited this process's own
(meaningless, in that child) standard-handle *values* rather than getting the console attachment's
own handles. Fixed with one flag (`STARTF_USESTDHANDLES`, all three handle fields left NULL),
verified against both the PowerShell regression test and the full `internal/sandbox/exec` relay
suite, merged (`4fd109a`), pushed, and confirmed CI green on `origin/main` (run `35631016860`,
including the real account-crossing e2e suite — `internal/e2e` took 113.992s in that run, evidence
it actually exercised the admin-gated tests, not skipped them).

A separate agent/process (not this session, found as an unexpected already-local commit) had, in the
meantime, produced `docs/reviews/security-performance-review-2026-09-20.md`: a full security/
performance review of the console-relay feature and adjacent code, merged to `main` as `d2c57f1`.
Verdict: **one release-blocking P0** (an unrestricted conhost process created before `Shield()`
narrows the stub's token, reachable and exploitable by the restricted-token program the stub later
starts — measured same-account with a real `CreateRemoteThread` proof of concept), the already-fixed
P1 (the `4fd109a` fix above, independently corroborated by the review, not re-opened), **7 P2s**
(relay-output-truncation-on-fast-exit, hard-link-enumeration-fail-open, Prune's O(N) redundant
SAM/LSA calls plus an unbounded SID leak, `Canonical`'s 64KiB-per-call heap allocation, `DedupeEntries`'
O(E²) pairwise directory walk, sweep's unbounded reorder buffer plus O(N·P) skip-root scan, and
tree-locks not canonicalizing drive aliases before hashing), and **2 P3s** (cleanup walking the whole
profile even for a narrow mask, and `command_launch`'s trace phase conflating launch with the whole
child lifetime).

The user asked (`/babygoal` + `/wrush`) to fix the whole review. Decomposed into 10 tasks (#212-#221)
respecting the review's own stated safe order plus file-overlap-driven sequencing, initially all
chained strictly sequentially. The user then asked to run independent tracks in parallel instead:
re-derived the true file-dependency DAG (only same-file tasks need to be sequential), deleted and
recreated tasks #214-#221 as five independent tracks — **exec** (P0-1→P2-1→P3-2, task #212→#213→#229,
files `internal/sandbox/exec/*.go`+`internal/win/proc/shield.go`), **pathid** (P2-2→P2-4, #222→#223,
`internal/win/pathid/pathid.go`+`internal/win/acl/links.go`), **sweep** (P2-3→P2-6, #224→#225,
`internal/policy/grant/prune.go`+`internal/win/acl/sweep.go`+`internal/win/sid/parse.go`+
`internal/win/acl/owner.go`), **profile** (P2-5→P3-1, #226→#227, `internal/policy/profile/*.go`),
**lock** (P2-7 solo, #228, `internal/base/lock/*.go`) — armed the pre-existing babysit cron (already
matched the default 15m interval, no new one needed), launched all five track-heads as concurrent
`rush run` sessions each in its own `/wrush`-style worktree, and personally zero-trust-verified and
merged each completed task before starting the next task in that same track.

**Completed and merged this session, in commit order on `main`:** P2-3 (`5e7a02c`), P2-7 (`e012214`),
P2-5 (`ffd6af0`), P2-2 (`616f4fa`), P2-6 (`8a263d7`), P2-4 (`976a2f7`), P3-1 (`f9cd049`) — 7 of the 9
remaining review tasks. The **pathid, sweep, profile and lock tracks are all fully done**. Every merge
was independently verified before landing: read the full diff, re-ran `go build`/`go vet`/`gofmt -l`/
`golangci-lint`/`go test -race` on the touched package(s) myself, then a full `go test ./...`, before
`git cherry-pick`ing the worktree's commit onto `main`. Caught and reverted nothing wrong this round —
every diff held up under scrutiny — but caught and correctly diagnosed **three separate transient,
load-induced test flakes** (`internal/cli/access`'s `TestARuleAndItsPermissionCannotDisagree` — a
300ms-window timing assumption; `internal/base/lock`'s a GC-pressure crash reported by the P3-1
sub-agent; `internal/win/proc`'s `TestTheProgramCannotTurnOnTheProcessThatConfinedIt` — an
`Access is denied` opening a thread for `WRITE_DAC`), all three reproduced 0-fails in isolation and
none touching the package whose merge triggered them — correctly attributed to genuinely heavy
concurrent system load (4-5 simultaneous `rush run` processes plus this session's own verification
test runs on one machine), not code regressions, and NOT worked around or silently dismissed — each
was isolated, reproduced clean, and only then accepted as environmental.

**Still in flight, both actively running rush sessions right now:**

- **Task #212 (P0-1, exec track head)** — the security-critical one. First attempt hit its 60m
  timeout mid-investigation (a genuinely valuable redesign, not wasted time): discovered the relay's
  own launch path creates a stub "birth console" via `CREATE_NO_WINDOW` BEFORE `takeConsoleRelay()`
  even runs, meaning there are actually **three** unrestricted-conhost-shaped targets to protect, not
  one — the relay's own conhost, `--own-console`'s `AllocConsole` host (same gap, never separately
  checked by the review either), and the stub's own birth-console host. Resumed with `--timeout 90m`
  and explicit instructions to implement (not further redesign) protection for all three, modeled on
  the existing `Shield()` (self-protection: process DACL + future-thread default DACL + every
  already-running thread) and `narrowBeforeResume` (the existing cross-process precedent, used today
  to narrow the stub itself from the operator side before `ResumeThread`) — reusing established
  patterns rather than inventing a new one. **Not yet complete as of this checkpoint.**
- **Task #230 (window-flash-fixes)** — a separate, NOT-review-related fix the user asked for mid-session
  via a second `/wrush` invocation: console windows briefly flashing/stealing focus during
  `go test ./...` and some CLI/subprocess launches, from a different (user-supplied, read-only)
  audit. Four findings: `internal/win/proc/driver_test.go`'s `AllocConsole`-then-`ShowWindow(SW_HIDE)`
  race (four interrupt tests affected), `internal/e2e/streams_test.go`'s deliberately-visible
  `--own-console` e2e test running on every ordinary `go test ./...` instead of being gated as
  interactive-only, dozens of ungoverned `exec.Command`/`CreateProcess` call sites needing a shared
  non-interactive-launch helper, and the npm wrapper's `windowsHide: false`. Explicitly scoped away
  from every file the concurrent P0-1 task owns. **Not yet complete as of this checkpoint.**

**Still pending, blocked, not yet started:** #213 (P2-1, exec track, blocked by #212), #229 (P3-2,
exec track, blocked by #213 — the whole exec track is serialized behind P0-1 landing first, since all
three share files in `internal/sandbox/exec`).

## Active goal

None set through `/goal`. TaskList drives the work.

## TaskList

### in_progress
- #212 P0-1: Protect the pre-Shield conhost from the restricted child (blockedBy: none) — resumed
  with `--timeout 90m` after a 60m timeout during redesign; implementing protection for three
  conhost-shaped targets (relay conhost, own-console conhost, stub birth-console), modeled on
  `Shield()`/`narrowBeforeResume`. Rush session id `p0-1-conhost-shield`, worktree
  `worktrees/p0-1-conhost-shield`.
- #230 Stop test/CLI runs from flashing visible console windows (blockedBy: none) — a separate
  user-requested fix, not part of the review. Rush session id `window-flash-fixes`, worktree
  `worktrees/window-flash-fixes`.

### pending
- #213 P2-1: Make relay output drain part of shutdown, not a fixed sleep (blockedBy: #212 — same
  files, `internal/sandbox/exec/console.go`+`stub.go`)
- #229 P3-2: Split command_launch trace phase into real lifecycle stages (blockedBy: #213 — same
  package, `internal/sandbox/exec`)

### recently completed (this session)
- #222 P2-2, #223 P2-4, #224 P2-3, #225 P2-6, #226 P2-5, #227 P3-1, #228 P2-7 — all 7 merged to
  `main`, see commit SHAs in the session summary above.
- (earlier, also this session) #211 Merge console-relay feature, push, get CI green — closed before
  the review-fix work began.

## Decisions

- **Parallelized the review-fix chain by the true file-dependency DAG**, not the review's own linear
  "safe order" section, once the user asked for parallelism — only tasks sharing a file need to be
  sequential; cross-track tasks (different files) are genuinely independent and were run concurrently
  in separate `/wrush` worktrees.
- **`TaskUpdate` cannot remove a `blockedBy` entry, only add one** — discovered when re-deriving the
  parallel DAG; the fix was deleting and recreating tasks #214-#221 (as #222-#229) with the correct
  dependencies from the start, rather than fighting an add-only API.
- **Every transient test failure this session was isolated and reproduced before being accepted as
  environmental** — none were dismissed on the "probably a flake" assumption alone; each was re-run
  solo/isolated and confirmed clean before merging past it. This is the same standing discipline as
  the CI-fix work before it.
- **P0-1's fix is modeled on existing precedent** (`Shield()`'s three-object self-protection,
  `narrowBeforeResume`'s cross-process narrowing-before-resume) rather than a novel mechanism —
  chosen deliberately when scoping the rush prompt, to reduce the risk surface on a release-blocking
  security fix.
- **The window-flash task is explicitly scoped away from every file P0-1 owns**, to avoid two
  concurrent agents touching `internal/win/proc` in ways that would conflict at merge time, even
  though both touch code adjacent to console/window creation.

## Open questions

- None blocking. Once #212 and #230 both land (each independently zero-trust-verified and merged the
  same way as the seven already done), the remaining exec-track chain (#213 then #229) runs the same
  way, and the babysit cron self-deletes once the TaskList is empty of pending/in_progress work.
- Whether to reinstall the newly-built wuserbox binary to the system once everything lands (the user
  has asked for this routinely in past sessions) — not requested yet for this specific build.

## Repo state

```
?? docs/checkpoints/2026-09-19-1938.md
```
```
f9cd049 Ask the cleanup walk whether a branch can hold a match before the walk opens it: a warm startup paid an enumeration of the whole profile for every cleanup list, though a rule scoped to one cache folder could reach a branch of three
976a2f7 Offer each resolution the room an ordinary path needs, and grow only to the room the answer names: every canonicalization and every hard-link enumeration handed a full 64 KiB to the heap whether the path was twelve characters or three hundred, and a pass over a large tree paid that per file before any byte moved. The first name of an enumeration now goes on the way the rest of its walk already does, and a final name past the ceiling the full-size buffer always refused past is refused still.
8a263d7 Hold the sweep's walk on credits only its writer returns, and carry the one skipped root still under it instead of the whole skip history
616f4fa Succeed only where the hard-link enumeration really ended
ffd6af0 Resolve each entry's place once per operation and compare by index: the pairwise dedupe walked the same directories for every comparison, and an already-copied profile paid the growing bill on every copy before a byte moved
e012214 Resolve a tree's root through the directory entry Windows itself answers to before any lock is taken: a SUBST drive spelled one real tree twice and the two grants hashed two lock files, each free to read, decide and write the same access list while the other was halfway through. An existing root that cannot be identified refuses the hold rather than standing on a lock of its own, and the chain is still claimed from the volume root downwards, so the collision closes without a crossing.
5e7a02c Resolve once, under lock, what taking a grant back re-derived per object
d2c57f1 Review sandbox security and performance after console relay changes
4fd109a Start the relay's child with NULL standard handles: values inherited from this process name nothing in the child's table, and the console attachment pays the child its own
6bb9e16 checkpoint: 2026-09-20-1546
ae06fee Hold the relay's kept pipe ends to the hygiene its comments claim: read back, not argued
```

`main` is pushed to `origin/main` only through `4fd109a` (the console-relay CI fix) — the entire
review-fix chain above (`5e7a02c` through `f9cd049`) is **local only, not yet pushed**, since the
chain is still in progress (#212/#230 in flight, #213/#229 pending). Two worktrees are currently
live and hold active rush sessions: `worktrees/p0-1-conhost-shield` (branch `p0-1-conhost-shield`,
still at its base `d2c57f1`, no commit yet) and `worktrees/window-flash-fixes` (branch
`window-flash-fixes`, still at its base `8a263d7`, no commit yet). The previous checkpoint file
(`docs/checkpoints/2026-09-19-1938.md`) remains untracked, left alone per this skill's own rule.
Several other worktrees exist as SIBLING directories outside `<repo-root>/worktrees/` (e.g.
`D:/dev/go/wuserbox-bootstrap-trace-xl`, `-hive-ci`, `-lease-lifecycle`, `-owner-cap-fix`, etc.) —
these are pre-existing, from earlier sessions, not touched or owned by this session; leave them alone.
