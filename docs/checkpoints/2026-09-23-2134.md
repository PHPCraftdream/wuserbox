# Checkpoint — 2026-09-23 21:34

## Session summary

Continued from the 2026-09-22-2330 checkpoint (rounds 5–8 of the security/performance review series, all closed). This session closed rounds 8 through 12 end-to-end using the same `/babygoal`+`/wrush` pattern established in prior sessions: decompose the review report into file-disjoint TaskList entries, arm `/babysit`, delegate each finding to a rush sub-agent in its own git worktree, personally zero-trust-verify every diff (read it line-by-line, rebuild build/vet/lint/`-race`/full-suite myself), merge, remove the worktree.

**Round 8** (5 findings, all merged): P2-1 reserved-guard alias-in-ancestor fix, P2-2 copy-preflight ordering, P3-1 partial-forget exclusions, P3-2 leaf revocation in retract's reverse index, P3-3 revoke scratch reuse. Three of the five rush sessions hit a `zai` provider peak-hours rate limit (08:00–12:00) — per rush.md's rule I never self-added `--allow-peak-hours`; the user explicitly authorized switching those three to direct `sh` (Sonnet 5 high-effort) subagents instead, which all completed successfully (one of them, P2-1, independently found and fixed a real bug: `pathSuspicious` misreading `.`/`..` as suspicious names).

Between rounds, an unattended interval produced a large uncommitted refactor sitting directly in the primary checkout (not mine) — the user confirmed it was "a neighboring agent" and asked me to check adequacy before committing. I found it split `fold.go`/`cleanup.go`/`mirror.go`/`place_test.go` into smaller files (matching CONTRIBUTING.md's line-count guidance) and implemented round-9's two P3 fixes, but round-10's own review (which had checked the identical uncommitted state) flagged a still-open P2 regression in it: `prepareDir` marked only `dst` as the mutation instead of walking up for a multi-component `MkdirAll`'s full missing-ancestor chain, unlike `file()`'s own already-correct walk. I fixed that gap myself directly (traced the exact code asymmetry, added `TestAMultiComponentDirectoryCreationRefreshesItsWholeCreatedChain`), ran the full verification battery, and committed it as `33dd8f4`. Pushed; CI ran twice for the same commit (apparent duplicate trigger) — one run green, one failed on two `internal/sandbox/exec` relay tests, which round 12 later explained precisely.

**Round 11** (2 findings, both merged): P2-1 — `placeResolver.snapshot`/`canonicalEntryPath`/`place()` collapsed every non-`IsNotExist` error into the same answer as "genuinely absent," so a temporarily-locked destination directory during copyEntries' missing-source branch could silently retire an existing record entry with no error. P3-1 — `refreshSnapshots`/`removeSnapshotChild`/alias-scan map rebuilds remained O(E²) in both CPU (unconditional slice filtering) and Go allocations (full map rebuild every scan), plus a real correctness bug where `canonicalComplete` mutated on a value-typed `dirSnapshot` copy never made it back into `r.dirs`.

**Round 12** (1 net-new finding after folding its P2-1 into round-11's task, both merged): P2-2 — the exact `internal/sandbox/exec` CI flakiness observed after round 8's push was root-caused as a genuine test-fixture race, not "just load": `prefixBarrierWriter.Write` had no signal for "the writer has actually entered its blocking wait," only a ready-file plus a fixed 200ms sleep, so a slow pour could let `finish()` legitimately return `nil` instead of `incomplete`. This directly corrected an earlier, premature conclusion of mine from this same session that the flake was purely environmental/load-induced — noted explicitly in the delegated task's own prompt as a self-correction. Round 12 also flagged a "neighbor path" of round-11's P2-1 through `reservedAtResolved`/`forget`/`Clear`/`NoAI`, which got folded into the same task rather than spawned separately.

Round-11/12's three resulting tasks (#264 P2-1, #265 P3-1 blocked on #264 for file overlap, #266 P2-2) were run two-parallel-plus-one-sequential. All three rush sessions hit repeated "queued behind an active run" failures (not peak-hours, not rate-limit) — per the established recovery procedure (reap → retry same session id up to 3×, then switch to a fresh `-v2` session id pointed at the same worktree without touching the in-progress diff) I recovered all three without losing any work; two of the three needed the `-v2` session-id switch after exactly 3 consecutive "queued" failures.

## Active goal

None set through `/goal`.

## TaskList

Empty — all of round 8/9/10/11/12's decomposed tasks (#259–#266) are completed and the TaskList was confirmed empty before this checkpoint. The babysit cron (`09e4e7d6`) self-deleted... actually was explicitly `CronDelete`d by me once the list was confirmed empty, matching the end-of-round pattern used throughout this session.

## Decisions

- **Treated the primary-checkout WIP refactor found mid-session as legitimate "neighbor agent" work to review and complete, not to discard** — per the user's explicit instruction ("проверь адекватность, если ок, то коммит"), verified it against round 10's own independent review of the identical state, found and fixed the one gap that review had flagged as still open, then committed.
- **Self-corrected an earlier premature diagnosis**: I had concluded mid-session (during round 8 verification) that `TestALiveConsolesHungCloseIsStillBoundedByTheCeiling`'s flakiness was purely load-induced and safe to ignore. Round 12's independent review found the real mechanism (a missing blocking-write signal in the test fixture, not just load), and I flagged my own earlier conclusion as wrong in the round-12 task's prompt rather than silently carrying the incorrect belief forward.
- **Folded round-12's P2-1 (neighbor path through Clear/NoAI) into round-11's existing P2-1 task** rather than creating a duplicate task, since round 12 explicitly characterized it as "the same defect, adjacent path," not a new independent finding.
- **On three consecutive "queued behind an active run" rush failures**, followed the established recovery procedure without deviation: reap the lock, retry the same session id up to two more times, and only switch to a fresh `-v2` session id (same worktree, follow-up prompt) after the third consecutive failure — recovered #264 and #265 this way without losing in-progress diffs.

## Open questions

- None blocking. `main` is 7 commits ahead of `origin/main` as of this checkpoint (`ffd744a`) — no push has been requested for this round-8-through-12 batch; the last explicit push/CI-check authorization was consumed for the earlier round-8 batch (`33dd8f4`, which did get pushed and had the round-9/10/11/12-documented CI duplicate-run split result).
- Two round-8-era CI runs for `33dd8f4` disagreed (one green, one red on the two relay tests round 12 diagnosed and round 12's own task, #266, then fixed) — that specific flake is now fixed and merged (`3c9116c`), but the *current* HEAD (`ffd744a`) has never itself been pushed to CI, so its own green/red status is unconfirmed pending a push.

## Repo state

```
(clean — git status --short returned nothing)
```
```
ffd744a Merge branch 'r11-quadratic-refresh'
3032a34 Close round 11's P3-1 quadratic refresh: take children out of a snapshot by name, extend the canonical index in place, and write the scan's completeness where the next rebuild reads it
9e8cb70 Merge branch 'r11-missing-source-unknown-state'
c33a81e Close round 11's P2-1 record loss over an unaskable destination and round 12's Clear that reported a take-back it never reached
c42b9e2 Merge branch 'r12-relay-test-oracle'
```

`main` is 7 commits ahead of `origin/main` (not pushed). Working tree clean. No worktrees remain from this session's `/wrush` work — every one was merged and removed (`git worktree list` shows only pre-existing, unrelated sibling worktrees from other sessions). TaskList empty, babysit cron removed.
