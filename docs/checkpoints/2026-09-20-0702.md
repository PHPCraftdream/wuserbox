# Checkpoint — 2026-09-20 07:02

## Session summary

Continued from the 2026-09-19-1938 checkpoint. Ran a second independent security review (`sx` agent, effort=xhigh) over the sandbox at `50c8db4`; it found nothing new (P0/P1/P2/P3 all clear), merged as `50c8db4`. The user then reported `wuserbox cx resume --last` (Codex CLI) failing inside the sandbox with "stdin is not a terminal" -- diagnosed live, on this machine, with a scratch probe sandbox (`worktrees/scratch/conpty-probe`, account `wub-25e03ccf5218477d`, UAC-approved by the user for this one experiment): `AttachConsole(ATTACH_PARENT_PROCESS)` from inside a sandbox account is measured "Access is denied" (the account cannot attach to the operator's console -- closed, not a bug to chase further), but `AllocConsole()` from inside the account succeeds and builds a real, working console (`GetConsoleMode` answers correctly on `CONIN$`/`CONOUT$` opened by name; `GetStdHandle` is never repointed at it, the same CRT-startup gap the stdin-bridge ConPTY test already found). Landed as `--own-console` (commit `f5c97b2` this session, via a real `rush run` after the user corrected me for using the `Agent` tool instead of the actual `rush` CLI the earlier `/wrush` sessions had already established as the standing tool): the stub calls `FreeConsole`→`AllocConsole`→opens `CONIN$`/`CONOUT$` by name and repoints its own `os.Stdin/Stdout/Stderr`, done *before* `Shield()` because `AllocConsole` under the post-Shield restricted token is itself "access denied" (measured) -- the unrestricted account token is what the console's own access check answers to. The user pushed back that a separate console window is a bad UX (wanted same-window, suggested a "unix-socket-style" pipe as an alternative) -- explained why no pipe/socket of any kind can substitute for a real console object (`isatty`/`GetConsoleMode` need the kernel console object specifically) and that `AttachConsole`/`STARTUPINFOEX` are both measured closed for the cross-account case; offered "check if Windows Terminal is the default terminal app, which may render the new console as a tab, not a separate top-level window" as the one remaining lever, not yet acted on.

Separately, the user asked to study, then fix, all findings from a `docs/reviews/release-review-P-2026-09-19-round10.md` review that had appeared on `main` from another parallel session. Personally re-verified both P1s before trusting the report: P1-1 (reserved-name tables miss `NTUSER.DAT.`/`NTUSER.DAT `/8.3-alias spellings that Windows resolves onto the real hive) confirmed live with a standalone `os.Root` probe; P1-2 (`runAsAccount`'s `bridge.finish()` deadlocking ahead of `j.Close()` when a program backgrounds a child) confirmed by tracing the actual defer order in `logon.go`, which I had just edited myself earlier in the session. Ran the whole round-10 backlog (2×P1, 6×P2, 8×P3 touched, one P3 doc-only) through real `rush run` invocations in worktrees, one per finding-cluster, with the same zero-trust verification discipline as `/wrush` (read every diff, rebuild, relint, retest myself before merging) -- landed as 15 commits between `7422dea` and `9c51665`. Notable along the way: two agents independently found and fixed bugs in their *own* fresh work during self-review (a nil-panic in the new reserved-name resolution code; a broken `--dir` short-path assumption); one deliberately overrode the review's literal suggested fix for P3-8 (`acl.Protect` would not have closed the hole -- it *intentionally* grants the read group access, which is the very capability being abused -- so a new `acl.ProtectOwnerOnly` was built instead); several `rush run` calls hit transient DNS/rate-limit failures and were resumed per rush.md's own guidance (re-run, don't fall back).

After `коммит, пуш, зелёный ci`, the actual GitHub Actions run came back red -- twice more after supposed fixes -- and each failure was chased to a genuine root cause rather than papered over, all traced to environment differences between this desk and the GitHub-hosted Windows runner: (1) `resolvedRootRel`'s new P1-1 code compared a canonicalized leaf path against an *unc*anonicalized `root.Name()`; the runner's own `TEMP` resolves through an 8.3 alias (`RUNNER~1`) so the comparison never lined up and the whole fix silently no-opped -- fixed by canonicalizing both sides, with a new regression test that reproduces the exact failure via `GetShortPathNameW` (confirmed against the unfixed code first: identical error text to the CI log); (2) `TestParseOptionsDefaultsToTheCurrentDirectory` compared against the raw `t.TempDir()` string instead of what `paths.Resolve` (P3-3's own new call) actually returns on a short-path machine -- test updated to resolve its own expectation the same way; (3) the new P1-2 e2e regression test (`TestARunComesBackWhenItsProgramLeavesAChildHoldingTheOutput`) failed with a bare, unexplained exit code 90 -- traced to the pseudo-console harness never draining the pty output pipe that carries the stub's own stderr, fixed that (real diagnostic value, not just for this bug), which revealed the actual error: "reading the slot handoff: ... Access is denied" -- root-caused to `newRealBox` (the shared e2e account fixture) never adding its account to the operator's per-owner read group the way a real `sandbox.Init` always does, so `acl.Protect`'s SDDL builder silently omits that group's ACE (the file's DACL is `PROTECTED_DACL`, so nothing inherited from an `openToEveryone`'d ancestor reaches it either -- tried that first, it did not help, which is itself a fact worth keeping). Fixed by having the test explicitly ensure the read-group exists and add its account to it, matching what `grants/read.go`'s `ensureReadable` does in production. Third push: CI fully green (`dacce9c`), including Delete boundary and Registry hive picture.

## Active goal

None set through `/goal`. TaskList drives the work, and is currently empty (last round-10 task closed and babysit self-deleted before this checkpoint).

## TaskList

Empty -- nothing pending or in progress.

### recently completed (round-10 review closure, this session)
- P1-1 reserved-name resolution (closed twice: the fix itself, then the short-path comparison bug CI found in it)
- P1-2 job/bridge teardown order (closed twice: the fix itself, then the e2e fixture's missing read-group membership CI found under it)
- P2-1/P2-2/P3-4 pathid identity for pinned-paths and narrow
- P2-3/P2-4/P2-5 three small sandbox-package bugs
- P2-6/P3-1 elevated --rm false success and swallowed exit codes
- P3-2/P3-7 fail-closed InsideSandbox and token/SID hygiene
- P3-3/P3-13 elevation flag/cwd carrying
- P3-5 config load validation
- P3-8/P3-9 slot-file read-group reachability
- P3-10/P3-11/P3-12/P3-14 CLI flag-parsing cluster
- `--own-console` (the live "stdin is not a terminal" fix)

## Decisions

- **`--own-console` gives the sandboxed program a genuinely separate console window** rather than attempting any form of shared/relayed console -- measured, not chosen for convenience: `AttachConsole` cross-account and `CreateProcessWithLogonW`+`STARTUPINFOEX` are both closed. The user has pushed back on the UX; no code change has been made in response yet, only an offered next step (check whether Windows Terminal-as-default renders it as a tab).
- **`rush run` (the actual CLI), not the `Agent` tool, is the standing mechanism for all `/wrush`-style delegated work in this repo**, per the user's explicit correction this session. Every round-10 fix and `--own-console` itself went through real `rush run` invocations in worktrees from this point on.
- **P3-8 was NOT fixed the way the review's own text suggested.** `acl.Protect` intentionally grants the per-owner read group access -- using it on the slot file would have kept the exact hole open. Built `acl.ProtectOwnerOnly` (no read-group ACE) instead.
- **Every CI-only failure this session was root-caused, not silenced or skipped.** All three traced to genuine environment differences (an 8.3-aliased `TEMP` on the GitHub-hosted runner, and an e2e fixture gap the new P1-2 test was the first to expose) -- none were flakes, and none were dismissed as such without independent reproduction first (the P1-1 regression test was confirmed to fail identically against the pre-fix code before being trusted).

## Open questions

- Whether to build anything further for the `--own-console` UX complaint (separate window). No decision requested yet beyond the Windows-Terminal-default check offered but not run.
- Other sessions may still be pushing to this same `main` in parallel, as they have been all session (round-10's own review commit `30de45c` was not authored in this window either).

## Repo state

```
?? docs/checkpoints/2026-09-19-1938.md
```
```
dacce9c Give the fixture's account the read-group membership a real one always has
ce6c90b Compare against the same resolution ParseOptions now applies
9258f75 Open the test's own state directory the same way its project directory already is
fb3680b Capture what the pseudo console printed, so a stub failure says why
7d0d917 Resolve the profile root itself before comparing, not only the leaf
```

`main` is pushed and in sync with `origin/main` at `dacce9c`; CI on that commit is green (Test, Delete boundary, and Registry hive picture all passed). The previous checkpoint file (`2026-09-19-1938.md`) is still untracked, same as when it was written -- left for the user, per the checkpoint skill's own rule.
